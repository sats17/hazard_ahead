1. Remove ID's from CSV.
2. Add single lane and double lane as additional hazard types. So we should not notify if speed breaker is in another lane.
3. Add a new field to the hazards table called "lane_type" which can take values "single" or "double".
4. Check if github support APK build process.
5. Add map UI which shows the scanning area and the hazards in that area.
6. Fix release build which does not work.